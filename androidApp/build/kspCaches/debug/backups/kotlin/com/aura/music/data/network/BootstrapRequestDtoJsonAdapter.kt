// Code generated by moshi-kotlin-codegen. Do not edit.
@file:Suppress("DEPRECATION", "unused", "UNUSED_PARAMETER", "ClassName", "REDUNDANT_PROJECTION",
    "RedundantExplicitType", "LocalVariableName", "RedundantVisibilityModifier",
    "PLATFORM_CLASS_MAPPED_TO_KOTLIN", "IMPLICIT_NOTHING_TYPE_ARGUMENT_IN_RETURN_POSITION")

package com.aura.music.`data`.network

import com.squareup.moshi.JsonAdapter
import com.squareup.moshi.JsonReader
import com.squareup.moshi.JsonWriter
import com.squareup.moshi.Moshi
import com.squareup.moshi.Types
import com.squareup.moshi.`internal`.Util
import java.lang.NullPointerException
import java.lang.reflect.Constructor
import kotlin.Int
import kotlin.String
import kotlin.Suppress
import kotlin.collections.Map
import kotlin.collections.emptySet
import kotlin.jvm.Volatile
import kotlin.text.buildString

public class BootstrapRequestDtoJsonAdapter(
  moshi: Moshi,
) : JsonAdapter<BootstrapRequestDto>() {
  private val options: JsonReader.Options = JsonReader.Options.of("device_id", "app_version",
      "capabilities")

  private val stringAdapter: JsonAdapter<String> = moshi.adapter(String::class.java, emptySet(),
      "deviceId")

  private val mapOfStringStringAdapter: JsonAdapter<Map<String, String>> =
      moshi.adapter(Types.newParameterizedType(Map::class.java, String::class.java,
      String::class.java), emptySet(), "capabilities")

  @Volatile
  private var constructorRef: Constructor<BootstrapRequestDto>? = null

  override fun toString(): String = buildString(41) {
      append("GeneratedJsonAdapter(").append("BootstrapRequestDto").append(')') }

  override fun fromJson(reader: JsonReader): BootstrapRequestDto {
    var deviceId: String? = null
    var appVersion: String? = null
    var capabilities: Map<String, String>? = null
    var mask0 = -1
    reader.beginObject()
    while (reader.hasNext()) {
      when (reader.selectName(options)) {
        0 -> deviceId = stringAdapter.fromJson(reader) ?: throw Util.unexpectedNull("deviceId",
            "device_id", reader)
        1 -> {
          appVersion = stringAdapter.fromJson(reader) ?: throw Util.unexpectedNull("appVersion",
              "app_version", reader)
          // $mask = $mask and (1 shl 1).inv()
          mask0 = mask0 and 0xfffffffd.toInt()
        }
        2 -> {
          capabilities = mapOfStringStringAdapter.fromJson(reader) ?:
              throw Util.unexpectedNull("capabilities", "capabilities", reader)
          // $mask = $mask and (1 shl 2).inv()
          mask0 = mask0 and 0xfffffffb.toInt()
        }
        -1 -> {
          // Unknown name, skip it.
          reader.skipName()
          reader.skipValue()
        }
      }
    }
    reader.endObject()
    if (mask0 == 0xfffffff9.toInt()) {
      // All parameters with defaults are set, invoke the constructor directly
      return  BootstrapRequestDto(
          deviceId = deviceId ?: throw Util.missingProperty("deviceId", "device_id", reader),
          appVersion = appVersion as String,
          capabilities = capabilities as Map<String, String>
      )
    } else {
      // Reflectively invoke the synthetic defaults constructor
      @Suppress("UNCHECKED_CAST")
      val localConstructor: Constructor<BootstrapRequestDto> = this.constructorRef ?:
          BootstrapRequestDto::class.java.getDeclaredConstructor(String::class.java,
          String::class.java, Map::class.java, Int::class.javaPrimitiveType,
          Util.DEFAULT_CONSTRUCTOR_MARKER).also { this.constructorRef = it }
      return localConstructor.newInstance(
          deviceId ?: throw Util.missingProperty("deviceId", "device_id", reader),
          appVersion,
          capabilities,
          mask0,
          /* DefaultConstructorMarker */ null
      )
    }
  }

  override fun toJson(writer: JsonWriter, value_: BootstrapRequestDto?) {
    if (value_ == null) {
      throw NullPointerException("value_ was null! Wrap in .nullSafe() to write nullable values.")
    }
    writer.beginObject()
    writer.name("device_id")
    stringAdapter.toJson(writer, value_.deviceId)
    writer.name("app_version")
    stringAdapter.toJson(writer, value_.appVersion)
    writer.name("capabilities")
    mapOfStringStringAdapter.toJson(writer, value_.capabilities)
    writer.endObject()
  }
}
