# AURA Server

FastAPI backend skeleton for AURA.

## Local run
1. Create a virtual environment.
2. Install dependencies from `pyproject.toml`.
3. Copy `.env.example` to `.env` and fill the values you need.
4. Run `uvicorn app.main:app --reload`.

## First endpoint
- `GET /health`

